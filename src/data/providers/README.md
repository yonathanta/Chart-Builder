# Data Providers
Provider implementations for fetching data based on chart specs (HTTP, DB adapters, cached sources). Providers receive normalized queries, handle authentication and caching, and return typed records without mutating chart state.
