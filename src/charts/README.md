# Charts
Vue components that host rendered charts and mediate between the D3 engine and higher-level UI (panels, layouts). Keep them declarative and spec-driven; they should accept validated chart specs and data references, not raw data blobs.
