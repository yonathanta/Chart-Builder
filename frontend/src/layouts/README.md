# Layouts
Reusable layout shells (dashboards, sidebars, grid panels) that compose chart components and UI panels. Keep layout state (panel sizing, responsive breakpoints) here to avoid coupling charts to the surrounding UI.
