# Specs
Schema and validators for chart specifications. Specs describe data bindings, encodings, layout, and styling while remaining data-agnostic. Keep specs serializable JSON to support storage, versioning, and server-side validation.
